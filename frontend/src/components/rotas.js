/*import React from "react";
import { BrowserRouter as Router, Routes, Route } from "react-router-dom";
import formEte from "../tratamentoETE";
import formLagoa from "../phLagoa";
import formStation from "../tratEst";
import Home from "./home";

const Rota = () => {
    return(
      <Router>
         <Routes>
         <Route path= "/" element={<Home />}></Route>
            <Route path= "/Lagoa" element={<formLagoa />}></Route>
             <Route path= "/ETE" element={< formETE />}></Route>
             <Route path= "/ETA" element={<formStation />}></Route>
         </Routes>
      </Router>
    );
};

export default Rota; */